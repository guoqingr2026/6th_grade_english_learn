# 家长操作指南（请勿告知学生密码）

## 积分规则（最新）

| 项目 | 说明 |
|------|------|
| **做题积分** | 系统自动判题获得（选择题、填空、动词等），无需密码 |
| **行为积分** | 家长记录课堂/生活习惯奖惩，需密码 |
| **累积积分** | 做题积分 + 行为积分 - 已兑换 |
| **积分兑换** | 只扣减累积积分，不扣做题积分；累积为 0 时不可兑换 |

## 设置家长密码

1. 输入学生昵称并点击「保存昵称」
2. 弹出窗口中设置密码，并再次确认
3. 若两次不一致，可选择「一键重置」重新开始（此时尚未设密码，可直接重置）

**密码存放位置（请家长记下）：**

- 保存在**本机浏览器**的 `localStorage` 中
- 存储键名：`english_grade6_profile_v1`
- 字段名：`parentPinHash`（加密摘要，**无法查看明文密码**）
- 侧栏 **「家长密码说明」**（仅家长模式可见）会显示当前是否已设置，以及上述存放位置
- 点「同步数据」后，会随学生昵称档案同步到局域网内**相同昵称**的设备

## 需要家长密码的操作

- 回答正确 / 回答错误（手动判题）
- 记录行为并计分
- 积分兑换
- **一键重置**（删除全部学习数据）

输入一次密码后，**本次浏览会话**内可继续使用上述功能（关闭页面后需重新输入）。

点击 **「退出家长模式」** 后，上述操作将恢复为每次点击都需输入密码。

## 进入家长模式

学生界面默认**不显示**侧栏的「家长密码说明」和「主机管理员」区域。

**方式一（常规）：** 进行需家长密码的操作（如积分兑换、行为记分）时输入密码，即进入家长模式；本会话内可点 **「退出家长模式」** 恢复学生界面。

**方式二（忘记密码时）：** 打开 **「每日感恩」** 标签，在感恩输入框中输入暗语 **`旭日长空光照人生`**（八个字，与页脚出品方一致），点 **「保存今天的感恩」**。系统会进入家长模式（**不会**把暗语显示在感恩墙上）。进入后可在侧栏使用「忘记密码 · 本机重设」等方式处理密码。

## 家长密码说明（仅家长模式可见）

进入家长模式后，展开侧栏 **「▼ 同步 / 记录 / 导入导出」**，可看到 **「家长密码说明」** 与 **「主机管理员」** 区域：

- 显示当前是否已设置家长密码
- 写明密码在 `localStorage` 中的键名与字段（见上文）
- **修改密码（需旧密码）**：记得旧密码时使用
- **忘记密码 · 本机重设**：仅在运行 `启动.bat` 的电脑本机可用（`http://localhost:8080`）

> 主机管理员密码与家长密码不是同一套（管理员只审课文/精讲改写）。

## 忘记家长密码（本机恢复）

须先**进入家长模式**（输入密码，或使用每日感恩暗语），再用电脑打开 **`http://localhost:8080`**（通过 `启动.bat` 启动，不要用局域网 IP）：

1. 展开侧栏 **「▼ 同步 / 记录 / 导入导出」**
2. 在 **「家长密码说明」** 中点击 **「忘记密码 · 本机重设」**
3. 确认后输入**新密码两次**

不会删除练习积分与错题本，只替换家长密码。

## 修改家长密码（需记得旧密码）

侧栏 **「家长密码说明」** → **「修改密码（需旧密码）」**：需先输入**当前**家长密码，再设新密码。

## 备用：隐藏入口（不推荐）

按住 **Alt** 键，在 2 秒内连续点击顶部 **「累积积分」** 标题或数字 **3 次**，可免旧密码设新密码。Windows 上 Alt 可能被浏览器占用导致无效，请优先使用侧栏 **「忘记密码 · 本机重设」**。

## 多端同步

- 右侧昵称下方 **「同步数据（多端刷新）」**
- **局域网地址**：侧栏自动列出本机 IP，点「复制」发给手机；手机顶栏同样有地址列表
- 须电脑运行 `启动.bat`，手机与 PC 同一 WiFi、相同昵称
- 练习后各端点一次同步；以最新修改为准

## 每日感恩（家长可选）

- 孩子可在 **「每日感恩」** 标签写中英双语感谢
- 家长验证后可添加 **鼓励语**（显示在孩子记录卡片上）
- 感恩记录会随「同步数据」在多端合并

## 已答对题目不再重复计分

系统自动判题时，同一道题此前已答对过，再次答对不再增加做题积分（仍可练习）。

## 一键重置

点击「一键重置」时需**输入两次家长密码**（第二次在确认弹窗之后），方可清空全部数据。

## 错题本

- 各模块系统自动收录答错题目
- 单词卡片：家长点击「回答错误」会记入错题本
- 打开 **「错题本」** 标签可查看全部错题、筛选模块、定向复习

## 练习与朗读（勿误导孩子）

- **单词卡片、语法、常用语、选择题、填空、动词专项、作文**：不会自动朗读，答对/答错后也不会自动跳题；需孩子自己点「下一张 / 下一题 / 下一组」
- **课文中英文讲读、知识点精讲**：默认不朗读；只有点工具栏 **「朗读」** 才会在本页内逐句向下读，读完本页即停，不会自动翻 Part
